# Tasks

- [x] 1 Move the System One page to `web/systemone/`, serve data at `/data/`, build the site from the root, update `_redirects`, `_headers` and `404.html`; add the shared navigation to every page
- [x] 2 Write the lab home (`web/index.html`): the lab, the experiment cards, how experiments are run
- [x] 3 Add the "How it works" explainer (SVG flow, shares, units, training) and the per-game "This game" panel from `web/explainers.ts`, with a test that input groups add up to each encoding size
- [x] 4 Add the "How it works" section to the handwriting page
- [x] 5 Verify in the browser (all pages, navigation, redirects, desktop and 375px, no console errors); run typecheck, tests, build and strict validation
