# Proposal

## Why

The author prefers the quadruped to start walking undisturbed, so that terrain, trot speed and the planner switch can be explored first.

## What Changes

- The random-pushes switch is off by default. The hint says that the experiments always push.

## Capabilities

### New Capabilities

### Modified Capabilities
- `demo-ui`: pushes switch off by default.

## Impact

- **Code:** `web/quadruped-controls.ts`, the System One page.
- **Results:** no experiment result changes.
