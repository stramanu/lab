{
  "name": "systemoneweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "System One in the browser: a vertical micro-model trained by an algorithmic System Two",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "train:snake": "tsx scripts/train-snake.ts",
    "eval:snake": "tsx scripts/eval-snake.ts",
    "bench:teacher": "tsx scripts/bench-teacher.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
